# 105. Construct BT from Inorder and Postorder
**难度**：Medium
**标签**：Tree, DFS,BFS,Binary Tree

## 题目
Given two integer arrays inorder and postorder where inorder is the inorder traversal of a binary tree and postorder is the postorder traversal of the same tree, construct and return the binary tree.


Example 1:

Input: inorder = [9,3,15,20,7], postorder = [9,15,7,20,3]
Output: [3,9,20,null,null,15,7]

## 思路
-找根节点：postorder 的最后一个元素永远是当前树（或子树）的根节点。

- 定位切口：在 inorder 中找到这个根节点的位置（假设索引为 i）。

- 划分左右：

在 inorder 中，i 左边的所有数就是左子树的成员。

在 inorder 中，i 右边的所有数就是右子树的成员。

- 递归构建：知道了左子树有多少个成员（假设有 k 个），我们就可以去 postorder 中切出接下来的 k 个数作为左子树，剩下的作为右子树。

## 代码（Python）

```python
        
class Solution(object):
    def buildTree(self, inorder, postorder):
        """
        :type inorder: List[int]
        :type postorder: List[int]
        :rtype: Optional[TreeNode]
        """
        if not postorder:
            return None
        
        root_val = postorder[-1]
        root = TreeNode(root_val)

        # 第三步: 找切割点.
        separator_idx = inorder.index(root_val)

        inorder_left = inorder[:separator_idx]
        inorder_right = inorder[separator_idx+1:]

        postorder_left = postorder[:len(inorder_left)]
        postorder_right = postorder[len(inorder_left): len(postorder) - 1]
        root.left = self.buildTree(inorder_left, postorder_left)
        root.right = self.buildTree(inorder_right, postorder_right)
         # 第七步: 返回答案
        return root
```

```
## 复杂度
- 时间复杂度：o(N)
- 空间复杂度：o(H)height
