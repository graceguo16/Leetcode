# 226. Invert Binary Tree
**难度**：Easy
**标签**：Tree, DFS,BFS,Binary Tree

## 题目
Given the root of a binary tree, invert the tree, and return its root.
Example 1:

Input: root = [4,2,7,1,3,6,9]
Output: [4,7,2,9,6,3,1]
## 思路
- DFS用递归，交换：先把当前节点的 left 和 right 换个位置。
- 递归：对交换后的左子树和右子树分别进行同样的操作。
- BFS把节点放进队列里。每次弹出一个节点，交换它的左右孩子。把它的左右孩子（如果有的话）再塞进队列，等下一轮处理。

## 代码（Python）

```python
class Solution(object):
    def invertTree(self, root):
        """
        :type root: TreeNode
        :rtype: TreeNode
        """
        # 基准情况：如果节点为空，直接返回
        if not root:
            return None
        
        # 核心动作：就像你说的，把左右对调
        # Python 的这种写法可以一行搞定交换
        root.left, root.right = root.right, root.left
        
        # 然后让下面的子节点也去完成它们的“对调”
        self.invertTree(root.left)
        self.invertTree(root.right)
        
        return root
        
class Solution(object):
    def invertTree(self, root):
        if not root:
            return None
            
        queue = deque([root])
        
        while queue:
            node = queue.popleft()
            
            # 交换当前节点的左右孩子
            node.left, node.right = node.right, node.left
            
            # 将子节点加入队列继续处理
            if node.left:
                queue.append(node.left)
            if node.right:
                queue.append(node.right)
                
        return root
```

```
## 复杂度
- 时间复杂度：o(N)
- 空间复杂度：o(H)height
